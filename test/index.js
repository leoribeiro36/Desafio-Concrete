const chai = requied('chai')
const expect = chai.expect

describe('Login', () => {
    it('Login', (done) => {
        const resultado = somanumeros(2,3)
        expect(resultado).be.equal(5)
        done()
    })
})